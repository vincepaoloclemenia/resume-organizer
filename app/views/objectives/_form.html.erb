<%= form_for ([person, person.objectives.build]) do |f| %>
    
        <div class="field">
            <%= f.label :description, class: "person-label"%>
            <%= f.text_area :description, size: "30x10", placeholder: "", class: "person-input"  %>
        </div>
        
        <div class="field">
            <% if controller.action_name == 'new' %>
                <%= f.submit 'Next', class: "btn register" %>
            <% elsif controller.action_name == 'edit' %>
                <%= f.submit 'Save', class: "btn register" %>
            <% end %>
        </div>
<% end %>