<nav class = "navbar navbar-default">
    <div class="container">
        <div class="navbar-header">
            <%= link_to "Resume Organizer", root_path, class: "navbar-brand" %> 
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="sr-only">Toggle Navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button> 
        </div>

        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav navbar-right">
                <% if controller.controller_name == 'page' && controller.action_name == 'home' %>
                    
                    <li><%= link_to 'Add New Resume', new_person_path %></li>                  
                    
                <% elsif resume_exist?(@person) %>
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><%= @person.last_name + ", "+@person.first_name%><span class="caret"></span></a>
                        <ul class="dropdown-menu" role="menu">
                            <li><%= link_to "Education", person_educations_path(@person) %><span class="warning"><em><%= @person.has_educations? %></em></span></li>
                            <li role="separator" class="divider"></li>
                            <li><%= link_to 'Skills', person_skills_path(@person) %><span class="warning"><em><%= @person.has_skills? %></em></span></li>
                            <li role="separator" class="divider"></li>
                            <li><%= link_to 'Objectives', person_objectives_path(@person) %><span class="warning"><em><%= @person.has_three_objectives? %></em></span></li>
                            <li role="separator" class="divider"></li>
                            <li><%= link_to 'Character References', person_character_references_path(@person) %><span class="warning"><em><%= @person.has_three_references? %></em></span></li>
                            <li role="separator" class="divider"></li>
                            <li><%= link_to 'Work Experience', person_character_references_path(@person) %></li>
                        </ul>   
                    </li>
                <% else %>
                    
                <% end %>
            </ul>
        </div>
    </div>
</nav>
